import PageHeader from "../components/ui/PageHeader";
export default function HomePage(){
    return (
        <><PageHeader title="About" description="Sevaxa was founded with a simple insight: if it's hard to report nonconformities, fewer people will do it. We created a system that removes the thresholds – so that every observation leads to improvement."/></>
    );
}